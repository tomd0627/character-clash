{
  "name": "goku-vs",
  "version": "1.0.0",
  "description": "Fictional Character Battle Analysis Web Application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --kill-others-on-exit",
    "dev:backend": "cd backend && npm run build && node dist/server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:init": "cd backend && npm run db:init",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": [
    "character",
    "battle",
    "analysis",
    "fictional",
    "comparison"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.8.1"
  }
}
